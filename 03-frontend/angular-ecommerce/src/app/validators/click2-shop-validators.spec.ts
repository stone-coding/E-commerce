import { Click2ShopValidators } from './click2-shop-validators';

describe('Click2ShopValidators', () => {
  it('should create an instance', () => {
    expect(new Click2ShopValidators()).toBeTruthy();
  });
});
